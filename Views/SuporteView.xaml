<UserControl x:Class="PDVModerno.Views.SuporteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Suporte Técnico" Foreground="{StaticResource TextPrimary}" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Converse diretamente com o painel central de administração e TI" Foreground="{StaticResource TextSecondary}" FontSize="13" Margin="0,3,0,0"/>
        </StackPanel>

        <!-- Indicador de Status de Conexão -->
        <Border Grid.Row="1" x:Name="BorderStatus" Background="#2A1B1B" CornerRadius="8" Padding="12,8" Margin="0,0,0,15" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="8" Height="8" Fill="{StaticResource DangerColor}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock x:Name="TxtStatusConexao" Text="Central de Suporte Offline - Verifique se a Central está aberta" Foreground="{StaticResource DangerColor}" FontSize="12" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>

        <!-- Conversa -->
        <Border Grid.Row="2" Background="{StaticResource BgPanel}" CornerRadius="12" BorderBrush="{StaticResource BgPanelLighter}" BorderThickness="1">
            <ScrollViewer x:Name="ScrollMessages" VerticalScrollBarVisibility="Auto" Padding="15">
                <ItemsControl x:Name="ListMensagens">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Balão da Mensagem -->
                                <Border x:Name="BubbleBorder" CornerRadius="10" Padding="10" MaxWidth="400" VerticalAlignment="Top">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Message}" Foreground="White" FontSize="13" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm'}" Foreground="#CCFFFFFF" FontSize="9" HorizontalAlignment="Right" Margin="0,3,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                            <DataTemplate.Triggers>
                                <!-- Operador (Alinhado à Esquerda, Cinza Escuro) -->
                                <DataTrigger Binding="{Binding Sender}" Value="Operador">
                                    <Setter TargetName="BubbleBorder" Property="HorizontalAlignment" Value="Left" />
                                    <Setter TargetName="BubbleBorder" Property="Background" Value="{StaticResource BgPanelLighter}" />
                                </DataTrigger>
                                <!-- Cliente/PDV (Alinhado à Direita, Roxo) -->
                                <DataTrigger Binding="{Binding Sender}" Value="Cliente">
                                    <Setter TargetName="BubbleBorder" Property="HorizontalAlignment" Value="Right" />
                                    <Setter TargetName="BubbleBorder" Property="Background" Value="{StaticResource PrimaryColor}" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Campo de Envio -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtMensagem" Height="40" Margin="0,0,10,0" VerticalContentAlignment="Center" KeyDown="TxtMensagem_KeyDown" GotFocus="TxtMensagem_GotFocus"/>
            <Button Grid.Column="1" Content="Enviar" Style="{StaticResource ModernButtonStyle}" Padding="25,10" Click="BtnEnviar_Click"/>
        </Grid>
    </Grid>
</UserControl>
