<UserControl x:Class="PDVModerno.Views.EstoqueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Controle de Estoque" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,20"/>

        <!-- Tabela (DataGrid) -->
        <Border Grid.Row="1" Background="{StaticResource BgPanel}" CornerRadius="10" Padding="15">
            <DataGrid x:Name="GridEstoque" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      HeadersVisibility="Column"
                      Background="Transparent"
                      RowBackground="{StaticResource BgPanelLighter}"
                      AlternatingRowBackground="{StaticResource BgPanel}"
                      Foreground="{StaticResource TextPrimary}"
                      BorderThickness="0"
                      GridLinesVisibility="None"
                      IsReadOnly="True"
                      FontSize="14">
                
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource BgDark}"/>
                        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                    <DataGridTextColumn Header="Cód. Barras" Binding="{Binding CodigoBarras}" Width="120"/>
                    <DataGridTextColumn Header="Produto" Binding="{Binding Nome}" Width="*"/>
                    <DataGridTextColumn Header="Categoria" Binding="{Binding Categoria}" Width="150"/>
                    <DataGridTextColumn Header="Preço" Binding="{Binding Preco, StringFormat='R$ \{0:N2\}'}" Width="120"/>
                    
                    <!-- Coluna Quantidade com formatação condicional simulada -->
                    <DataGridTemplateColumn Header="Qtd Estoque" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="5,2" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#1A00C853"/> <!-- Fundo Verde transparente -->
                                            <Style.Triggers>
                                                <!-- Se Estoque baixo, fundo vermelho -->
                                                <DataTrigger Binding="{Binding QuantidadeEstoque}" Value="0">
                                                    <Setter Property="Background" Value="#1AFF3D00"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding QuantidadeEstoque}" FontWeight="Bold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{StaticResource SuccessColor}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding QuantidadeEstoque}" Value="0">
                                                        <Setter Property="Foreground" Value="{StaticResource DangerColor}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
