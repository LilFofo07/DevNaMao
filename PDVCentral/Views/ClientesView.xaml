<UserControl x:Class="PDVCentral.Views.ClientesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Título -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Configuração de Clientes" Foreground="{StaticResource TextPrimary}" FontSize="26" FontWeight="Bold"/>
            <TextBlock Text="Cadastre novos clientes e gerencie suas credenciais de pagamento Mercado Pago" Foreground="{StaticResource TextSecondary}" FontSize="14" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Barra de Ações Superior -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Campo de Pesquisa -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Pesquisar:" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14"/>
                <TextBox x:Name="TxtFiltro" Width="300" Height="36" VerticalContentAlignment="Center" TextChanged="TxtFiltro_TextChanged"/>
            </StackPanel>

            <!-- Botão Novo Cliente -->
            <Button Grid.Column="1" Style="{StaticResource SuccessButtonStyle}" Content="+ Novo Cliente" Padding="20,10" Click="BtnNovoCliente_Click"/>
        </Grid>

        <!-- Grid de Clientes -->
        <Border Grid.Row="2" Background="{StaticResource BgPanel}" CornerRadius="12" BorderBrush="{StaticResource BgPanelLighter}" BorderThickness="1">
            <Grid Margin="20">
                <DataGrid x:Name="GridClientes" Background="Transparent" Foreground="White" BorderThickness="0" 
                          AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="None" RowBackground="Transparent" AlternatingRowBackground="#08FFFFFF">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#1E1E2D"/>
                            <Setter Property="Foreground" Value="#9499B3"/>
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BgPanelLighter}"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="12,12"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                            <ContentPresenter />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#252538"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nome do Cliente" Binding="{Binding Nome}" Width="200" />
                        <DataGridTextColumn Header="CNPJ" Binding="{Binding Cnpj}" Width="150" />
                        <DataGridTextColumn Header="E-mail" Binding="{Binding Email}" Width="180"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                        <DataGridTemplateColumn Header="Ações" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Editar" Margin="0,0,5,0" Style="{StaticResource ModernButtonStyle}" Padding="10,5" Click="BtnEditar_Click" Tag="{Binding}"/>
                                        <Button Content="Bloquear/Ativar" Margin="0,0,5,0" Style="{StaticResource ModernButtonStyle}" Background="#FF8800" Padding="10,5" Click="BtnToggleStatus_Click" Tag="{Binding}">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                        <Button Content="Excluir" Style="{StaticResource DangerButtonStyle}" Padding="10,5" Click="BtnExcluir_Click" Tag="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
