<UserControl x:Class="PDVCentral.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Painel Lateral: Lista de Clientes -->
        <Border Grid.Column="0" Background="{StaticResource BgPanel}" CornerRadius="12,0,0,12" BorderBrush="{StaticResource BgPanelLighter}" BorderThickness="1,1,0,1">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Clientes / Suporte" Foreground="{StaticResource TextPrimary}" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <ListBox x:Name="ListClientesConversa" Grid.Row="1" Background="Transparent" BorderThickness="0" SelectionChanged="ListClientesConversa_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="ItemBorder" CornerRadius="8" Padding="12" Margin="0,0,0,8" Background="Transparent">
                                <StackPanel>
                                    <TextBlock Text="{Binding Nome}" Foreground="{StaticResource TextPrimary}" FontSize="14" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Cnpj}" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,3,0,0"/>
                                </StackPanel>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource BgPanelLighter}" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>

        <!-- Painel de Conversa Ativa -->
        <Border Grid.Column="1" Background="{StaticResource BgPanelLighter}" CornerRadius="0,12,12,0" BorderBrush="{StaticResource BgPanelLighter}" BorderThickness="0,1,1,1">
            <Grid x:Name="ChatPanel" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="65" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Cabeçalho da Conversa -->
                <Border Grid.Row="0" Background="{StaticResource BgPanel}" BorderBrush="{StaticResource BgDark}" BorderThickness="0,0,0,1" Padding="20,10">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="TxtNomeChat" Text="Conversa com o Cliente" Foreground="{StaticResource TextPrimary}" FontSize="18" FontWeight="Bold"/>
                        <TextBlock x:Name="TxtCnpjChat" Text="CNPJ/CPF: 000.000.000-00" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Área de Mensagens -->
                <ScrollViewer Grid.Row="1" x:Name="ScrollMessages" VerticalScrollBarVisibility="Auto" Padding="20">
                    <ItemsControl x:Name="ChatMessagesList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Bolha da Mensagem -->
                                    <Border x:Name="BubbleBorder" CornerRadius="12" Padding="12,10" MaxWidth="450" VerticalAlignment="Top">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Message}" Foreground="White" FontSize="14" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm'}" Foreground="#CCFFFFFF" FontSize="10" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                                <DataTemplate.Triggers>
                                    <!-- Operador (Alinhado à Direita, Roxo) -->
                                    <DataTrigger Binding="{Binding Sender}" Value="Operador">
                                        <Setter TargetName="BubbleBorder" Property="HorizontalAlignment" Value="Right" />
                                        <Setter TargetName="BubbleBorder" Property="Background" Value="{StaticResource PrimaryColor}" />
                                    </DataTrigger>
                                    <!-- Cliente (Alinhado à Esquerda, Escuro) -->
                                    <DataTrigger Binding="{Binding Sender}" Value="Cliente">
                                        <Setter TargetName="BubbleBorder" Property="HorizontalAlignment" Value="Left" />
                                        <Setter TargetName="BubbleBorder" Property="Background" Value="{StaticResource BgPanel}" />
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Campo de Entrada -->
                <Border Grid.Row="2" Background="{StaticResource BgPanel}" BorderBrush="{StaticResource BgDark}" BorderThickness="0,1,0,0" Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtInputMsg" Height="40" Margin="0,0,10,0" VerticalContentAlignment="Center" KeyDown="TxtInputMsg_KeyDown" GotFocus="TxtInputMsg_GotFocus"/>
                        <Button Grid.Column="1" Content="Enviar" Style="{StaticResource ModernButtonStyle}" Padding="20,10" Click="BtnEnviar_Click"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- Mensagem de Seleção Vazia -->
        <TextBlock x:Name="TxtEmptyChat" Grid.Column="1" Text="Selecione um cliente ao lado para iniciar o atendimento." 
                   Foreground="{StaticResource TextSecondary}" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20"/>
    </Grid>
</UserControl>
