<UserControl x:Class="PDVCentral.Views.MaintenanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="180" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Título -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Manutenção do Sistema" Foreground="{StaticResource TextPrimary}" FontSize="26" FontWeight="Bold"/>
            <TextBlock Text="Efetue cópias de segurança do banco de dados, limpezas e testes de conectividade em tempo real" Foreground="{StaticResource TextSecondary}" FontSize="14" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Painel de Ações de Manutenção -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <!-- Card Informações do Banco -->
            <Border Grid.Column="0" Background="{StaticResource BgPanel}" CornerRadius="12" BorderBrush="{StaticResource BgPanelLighter}" BorderThickness="1" Margin="0,0,15,0" Padding="20">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Status do Banco de Dados" Foreground="{StaticResource TextSecondary}" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="TxtDbStatus" Text="Conectado (SQLite)" Foreground="{StaticResource SuccessColor}" FontSize="18" FontWeight="Bold" Margin="0,8,0,0"/>
                    
                    <Separator Height="1" Background="{StaticResource BgPanelLighter}" Margin="0,12"/>
                    
                    <TextBlock Text="Tamanho do Arquivo:" Foreground="{StaticResource TextSecondary}" FontSize="12"/>
                    <TextBlock x:Name="TxtDbSize" Text="0 KB" Foreground="{StaticResource TextPrimary}" FontSize="15" FontWeight="SemiBold" Margin="0,3,0,0"/>
                </StackPanel>
            </Border>

            <!-- Card Botões de Ação -->
            <Border Grid.Column="1" Background="{StaticResource BgPanel}" CornerRadius="12" BorderBrush="{StaticResource BgPanelLighter}" BorderThickness="1" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Ações Disponíveis" Foreground="{StaticResource TextPrimary}" FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>

                    <UniformGrid Grid.Row="1" Columns="3" Rows="1">
                        <Button Style="{StaticResource SuccessButtonStyle}" Margin="0,0,10,0" Padding="10" Click="BtnBackup_Click">
                            <StackPanel>
                                <TextBlock Text="Criar Backup" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="Salvar central.db" FontSize="10" Foreground="#E0FFFFFF" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource DangerButtonStyle}" Margin="0,0,10,0" Padding="10" Click="BtnLimparLogs_Click">
                            <StackPanel>
                                <TextBlock Text="Limpar Histórico" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="Limpar vendas/logs" FontSize="10" Foreground="#E0FFFFFF" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource ModernButtonStyle}" Background="#3E4756" Padding="10" Click="BtnDiagnostico_Click">
                            <StackPanel>
                                <TextBlock Text="Auto-Diagnóstico" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="Testar conectividade" FontSize="10" Foreground="#E0FFFFFF" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Button>
                    </UniformGrid>
                </Grid>
            </Border>
        </Grid>

        <!-- Console / Terminal de Log de Diagnóstico -->
        <Border Grid.Row="2" Background="#0C0C0F" CornerRadius="12" BorderBrush="{StaticResource BgPanelLighter}" BorderThickness="1">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Console de Diagnósticos e Logs de Manutenção" Foreground="{StaticResource TextSecondary}" FontSize="13" FontWeight="Bold" Margin="0,0,0,10"/>

                <ListBox x:Name="ListTerminal" Grid.Row="1" Background="Transparent" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="13" Foreground="#00FF66" TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Margin" Value="0,2"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
